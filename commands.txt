# Cargo

cargo watch -x run
cargo run
cargo test -- --no-capture
cargo watch -x "test" -x "run"
cargo watch -x "test"

# Pomonote

"quit" | "exit" | "q"
"remove" | "rm"
"complete" | "done"
"start"
"stop"